# Rebuild log — 2026-08-06

`main.tex` is untouched.  The rebuilt manuscript is `main_new.tex`
(compiles clean: 0 errors, 0 undefined references, 44 citations, 38 pages).

## How to regenerate

```
python3 hebrew/make_tables.py        # tab_corpus.tex, tab_targets.tex
python3 methods_paper/make_fix_tables.py   # tab_leakage.tex, tab_subsources.tex
python3 methods_paper/splice.py      # main.tex + fragments -> main_new.tex
python3 methods_paper/patch_intro.py # prose patches (17, in 3 passes)
```

Every script fails loudly on a missing anchor rather than silently skipping,
so a future edit to `main.tex` that breaks a splice point will halt the build.

## Title

Was: *A multivariate Bayesian framework for diachronic dating of ancient
texts: Validation on Biblical Hebrew and Ancient Greek*

Now: *Ordering without dating: a leakage-free reassessment of linguistic
chronology in Biblical Hebrew*

## Sections removed

| section | lines | reason |
|---|---|---|
| Archaism detection: the resistant model | ~170 | `n_feat_overshoot = 0` on all 44 units; instrument does not fire |
| Genre correction | ~60 | Strategies B+D results superseded; genre now handled as a measured residual effect in Results |
| Validation (old) | ~181 | replaced wholesale |

Ten float environments pruned: fig3 (oracle Jeremiah), fig4 (full vs
resistant), fig5/fig6 (genre), fig7 (HB-VI vs MLE-MVN), fig9 (Greek
validation), fig_loo_calibration, fig_s8 (calibration), fig_s10 (CI vs word
count), fig_s11 (key posteriors).

## Sections added or rewritten

- **Abstract** — rewritten to the bounded claim.
- **Introduction** — contribution list, results summary and roadmap replaced.
- **Training corpus** — rewritten; Table 1 regenerated from
  `corpus_manifest_v2.json` (13 date/sigma corrections, all word counts,
  Lamentations and Ecclesiastes added).
- **Residual covariance and regularization** — Tikhonov justification
  rewritten: with J=14 < N=23 the covariance is no longer rank-deficient,
  so the old motivation does not apply.  Regularization retained for
  eigenvalue shrinkage instead.
- **Prior sensitivity / Results** — rewritten around Δ_AB as a diagnostic of
  prior influence.  New `tab:subsources` reports Δ_AB only; absolute MAPs
  from that run are superseded and omitted.
- **Model-independent feature scoring** — rescued from the cut archaism
  section.  Corpus-v2 ordering is D (−0.30) < P (+0.15) < JE (+0.53), which
  reverses the previously reported D < JE < P.  Presented as descriptive,
  with the instability across corpus versions stated.
- **Validation** — new.  Prior-leakage failure mode with algebra and
  `tab:leakage`; LOO protocol; what the signal supports and does not.
- **Results: dating the undated units** — new.  Post-exilic result,
  what cannot be claimed, the archaic poems, genre as a measured confound.
- **Conclusion** — rewritten.

## Prose patches applied (17)

Title x2, contribution list, results summary, roadmap, limitations genre
passage, extensibility passage, cross-linguistic paragraph, HC comparison
x2, orphaned S11 stub, S12 caption counts x2, background resistant-model
paragraph, conservative-upper-bound passage, Tier-3 description,
feature-tier table row.

Stale-claim sweep is clean: no remaining occurrences of the old P date
(402--406), old D Code date (698--737), old holdout MAE (16.8), the Greek
63-text/five-holdout claims, "archaism diagnostic", "Strategies B+D",
"resistant model", or the old 22-text corpus count.

## Still outstanding

1. **Figures are not regenerated.**  Eight survive and are included, but
   `fig1_corpus_timeline` and `fig_s12_corpus_timeline` still plot HB-VI MAP
   estimates from the leaky run and must be redrawn before submission.
   `fig2` (feature correlations), `fig8` (prior sensitivity),
   `fig_circularity`, `fig_s6`, `fig_s7`, `fig_s9` are unaffected.
2. **A figure for the ordering result does not yet exist.**  The central
   finding has no visual.  A true-vs-predicted scatter with conformal bands
   and period shading is the obvious candidate.
3. **S-tables** S2/S3/S4/S5 not yet regenerated against corpus v2.
4. **Cover letter** still describes the withdrawn Greek validation and the
   reviewer-exclusion request; needs rewriting to the new claim.
5. **Greek pipeline** untouched.  Rerunning it under the LOO/agnostic-prior
   design is the single highest-value remaining experiment: at n=63 it may
   support what n=25 cannot.
